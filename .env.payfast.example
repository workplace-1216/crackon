# PayFast Configuration
# Copy these to your .env.local file and fill in the values

# PayFast Sandbox Configuration (for development)
# Get these from: https://sandbox.payfast.co.za/
PAYFAST_SANDBOX_MERCHANT_ID=10041801
PAYFAST_SANDBOX_MERCHANT_KEY=macbun2ckwoii
PAYFAST_SANDBOX_PASSPHRASE=Steep-Snoring-Backdrop

# PayFast Production Configuration (for production)
# Get these from your PayFast merchant account
PAYFAST_MERCHANT_ID=your_production_merchant_id
PAYFAST_MERCHANT_KEY=your_production_merchant_key
PAYFAST_PASSPHRASE=your_production_passphrase

# PayFast URLs - Update these with your actual domain
# For local development with ngrok:
# 1. Run: ngrok http 3000
# 2. Update PAYFAST_NOTIFY_URL with your ngrok URL

# Onboarding flow URLs (goes to dashboard)
PAYFAST_RETURN_URL=http://localhost:3000/api/payment/success
PAYFAST_CANCEL_URL=http://localhost:3000/api/payment/cancel

# Billing flow URLs (returns to billing page)
PAYFAST_BILLING_RETURN_URL=http://localhost:3000/api/payment/billing-success
PAYFAST_BILLING_CANCEL_URL=http://localhost:3000/api/payment/billing-cancel

# Webhook URL (same for all flows)
PAYFAST_NOTIFY_URL=https://your-ngrok-url.ngrok.io/api/webhook/payfast

# For production:
# PAYFAST_RETURN_URL=https://yourdomain.com/api/payment/success
# PAYFAST_CANCEL_URL=https://yourdomain.com/api/payment/cancel
# PAYFAST_BILLING_RETURN_URL=https://user-admin.crackon.ai/api/payment/billing-success
# PAYFAST_BILLING_CANCEL_URL=https://user-admin.crackon.ai/api/payment/billing-cancel
# PAYFAST_NOTIFY_URL=https://yourdomain.com/api/webhook/payfast